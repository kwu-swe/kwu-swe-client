[build]
command = "npm install && npm install @rollup/rollup-linux-x64-gnu && npm run build"
publish = "build"

[build.environment]
  NODE_OPTIONS = "--max_old_space_size=4096"

[[redirects]]
from = "/*"
to = "/index.html"
status = 200